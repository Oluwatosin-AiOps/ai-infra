# Fraud Detection API — copy to .env and adjust
# For local dev: CORS must include your frontend origin (Vite default = 5173).
PROJECT_NAME=Fraud Detection API
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# SENTRY_DSN=  # optional
# ENVIRONMENT=local
