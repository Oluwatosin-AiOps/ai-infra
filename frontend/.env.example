# Backend API base URL (no trailing slash).
# Local dev: leave unset to use Vite proxy (frontend proxies /api to 127.0.0.1:8000; no CORS).
# Or set to http://127.0.0.1:8000 if not using proxy (backend must allow CORS from http://localhost:5173).
# Production: your Backend ALB URL (e.g. https://api.example.com)
# VITE_API_URL=http://localhost:8000
